import { createTheme } from "@material-ui/core";

const theme = createTheme({
  palette: {
    primary: {
      main: "#0026ca",
      //   main: "#14213d",
    },
    secondary: {
      main: "#1DE9B6",
    },
    common: {
      drawer: "#A22032",
    },
  },
});

export default theme;
